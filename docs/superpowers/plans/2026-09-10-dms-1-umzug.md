# DMS 1 — Umzug, Datenmodell und Migration (Implementierungsplan)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Das Modul `dms` existiert mit allen fünf Tabellen, die Akte ist aus dem Kern ausgezogen, und keine vorhandene Zeile ist dabei verloren gegangen.

**Architecture:** Der Kern behält die Pipeline ohne Ablage (`exportDocument`), das Modul übernimmt `documents` samt Nummernvergabe, Storno und Lesen. Weil `drizzle.config.ts` die Modulschemas per Glob (`../modules/*/src/schema.ts`) mitnimmt, ist der Umzug einer Tabelle von Datei zu Datei für `db:generate` unsichtbar — nur die Spaltenänderungen erscheinen im Diff. Der Umzug der Bezüge nach `document_links` ist eine handgeschriebene Datenmigration nach dem Muster von `0006_i18n_locales.sql`.

**Tech Stack:** TypeScript, Drizzle (SQLite), Zod 4, Vitest.

**Spec:** `docs/superpowers/specs/2026-09-10-dokumente-und-korrespondenz-design.md` (§ 4 Schnitt, § 5 Datenmodell)

## Global Constraints

- Code Englisch, Oberflächentexte über i18n. Kein hartcodierter UI-Text (`AGENTS.md`, Prinzip 7).
- IDs über `newId()` (ULID). Zeit über `deps.clock.now()` bzw. `isoNow(deps.clock)` — nie `new Date()` in Fachcode.
- Fachfehler sind `Result`-Werte (`forbidden`, `validation`, `notFound`, `conflict`), nie Exceptions.
- Service-Signatur `fn(deps, ctx, input) → Promise<Result<T>>`; Ablauf `requirePermission` → `validate` → `db.transaction` → `recordAudit` → `ok`.
- Migrationen entstehen mit `pnpm --filter @kompass/core db:generate` und werden nie nachträglich editiert. Handgeschriebene Datenmigrationen sind eigene Dateien mit eigenem Journal-Eintrag.
- TDD: kein Produktionscode ohne zuvor rot gesehenen Test.
- Vor dem Push `pnpm verify`. Docker liegt auf diesem Rechner unter `/Applications/Docker.app/Contents/Resources/bin` und ist eventuell nicht im PATH.

---

### Task 1: Prüfsumme an `media_assets`

Das PDF ist der rechenschaftsrelevante Datensatz, nicht die Datenbankzeile (Entscheidung 3). Dafür braucht jede Datei ihre vollständige SHA-256. Der Hash wird in `prepare()` bereits berechnet (`packages/core/src/media/service.ts:75`) und heute nur für die ersten zwölf Stellen im Dateinamen benutzt.

**Files:**
- Modify: `packages/core/src/db/schema.ts` (`mediaAssets`)
- Modify: `packages/core/src/media/service.ts` (`storeMediaInternal`)
- Create: `packages/core/src/db/migrations/0013_media_checksum.sql` (erzeugt)
- Test: `packages/core/tests/media.test.ts`

**Interfaces:**
- Consumes: nichts
- Produces: `mediaAssets.checksum` (Spalte `checksum`, `text`, nullable) — gefüllt bei jedem neuen Asset; `MediaAssetRecord.checksum: string | null`

- [ ] **Step 1: Failing Test schreiben**

In `packages/core/tests/media.test.ts` ergänzen:

```ts
it('speichert die vollständige SHA-256 der Datei', async () => {
  const { deps, ctx } = setup();
  const bytes = pngBytes();
  const stored = await storeMediaAsset(deps, ctx, { originalName: 'bild.png', bytes, declaredMimeType: 'image/png' });
  expect(stored.ok).toBe(true);
  if (!stored.ok) return;
  const expected = createHash('sha256').update(bytes).digest('hex');
  expect(stored.value.checksum).toBe(expected);
  expect(stored.value.checksum).toHaveLength(64);
});
```

`createHash` aus `node:crypto` importieren; `pngBytes()` und `setup()` aus der vorhandenen Datei übernehmen (dort gibt es bereits Helfer für Testbytes — den vorhandenen Namen verwenden, nicht neu erfinden).

- [ ] **Step 2: Test rot sehen**

Run: `pnpm --filter @kompass/core test -- media`
Expected: FAIL — `checksum` existiert nicht am Ergebnistyp.

- [ ] **Step 3: Spalte ergänzen**

In `packages/core/src/db/schema.ts` bei `mediaAssets` nach `bytes`:

```ts
  /** Vollständige SHA-256, hexadezimal. Der Dateiname trägt nur die ersten zwölf Stellen. */
  checksum: text('checksum'),
```

Nullable, weil vorhandene Zeilen sie nicht haben; für neue Zeilen ist sie immer gefüllt.

- [ ] **Step 4: Beim Ablegen füllen**

In `packages/core/src/media/service.ts`, im `insert` von `storeMediaInternal`, `checksum: meta.hash` in die Werte aufnehmen.

- [ ] **Step 5: Migration erzeugen**

Run: `pnpm --filter @kompass/core db:generate`
Die erzeugte Datei in `0013_media_checksum.sql` umbenennen und den `tag` im letzten Eintrag von `packages/core/src/db/migrations/meta/_journal.json` auf `0013_media_checksum` setzen — Hausbrauch, siehe `0012_contacts`.

- [ ] **Step 6: Tests grün sehen**

Run: `pnpm --filter @kompass/core test -- media`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add packages/core/src/db packages/core/src/media packages/core/tests/media.test.ts
git commit -m "feat(media): full sha-256 checksum on every stored asset"
```

---

### Task 2: Vertragsänderung — `type` statt `prefix`, Entwurfskennzeichnung

Das Nummernpräfix wandert von der Vorlage zur Dokumentart (Entscheidung 18), und die Vorschau braucht ein Wasserzeichen (§ 6.1). Beides sind Änderungen am Kernvertrag, die vor dem Modul passieren müssen.

**Files:**
- Modify: `packages/core/src/modules/manifest.ts` (`DocumentTemplate`, `DocumentSlots`, `defineModule`)
- Modify: `packages/documents/src/templates.ts` (`coreDocumentTemplates`)
- Modify: `packages/documents/templates/bases/a4-plain.typ`, `a4-mit-briefkopf.typ`, `a4-ohne-briefkopf.typ`
- Test: `packages/core/tests/modules.test.ts`, `packages/documents/tests/bases-render.test.ts`

**Interfaces:**
- Consumes: nichts
- Produces:
  - `DocumentTemplate.type: string` (Schlüssel einer Dokumentart, `^[a-z][a-z0-9-]*$`); `prefix` entfällt
  - `DocumentSlots.draft?: boolean`
  - Basen zeichnen bei `draft: true` ein Wasserzeichen

- [ ] **Step 1: Failing Test für den Vertrag**

In `packages/core/tests/modules.test.ts`:

```ts
it('lehnt eine Dokumentart mit ungültigem Schlüssel ab', () => {
  expect(() =>
    defineModule({
      key: 'demo', version: '1.0.0', permissions: [],
      documentTemplates: [{ key: 'letter', type: 'Brief!', schema: z.object({}), base: 'a4-plain', build: () => ({ slots: { kind: 'plain' }, body: { markdown: '' } }) }],
    }),
  ).toThrow(/invalid document type/);
});
```

- [ ] **Step 2: Test rot sehen**

Run: `pnpm --filter @kompass/core test -- modules`
Expected: FAIL — `type` ist am Typ unbekannt, `prefix` fehlt.

- [ ] **Step 3: Vertrag ändern**

In `packages/core/src/modules/manifest.ts`: In `DocumentTemplate` `prefix` durch

```ts
  /** Schlüssel der Dokumentart. Sie trägt Nummernpräfix und Fristklasse. */
  type: string;
```

ersetzen. In `DocumentSlots` ergänzen:

```ts
  /** Vorschau eines Entwurfs: die Basis zeichnet ein Wasserzeichen. */
  draft?: boolean;
```

In `defineModule` `TEMPLATE_PREFIX` durch `TEMPLATE_TYPE = /^[a-z][a-z0-9-]*$/` ersetzen und die Prüfung auf `template.type` umstellen, Fehlertext `invalid document type: ${template.type}`.

- [ ] **Step 4: Kernvorlagen anpassen**

In `packages/documents/src/templates.ts`: bei `letterhead` `prefix: 'BRF'` → `type: 'letter'`, bei `auditExport` `prefix: 'PRO'` → `type: 'audit-export'`. Zu `audit-export` gibt es bewusst keine Dokumentart: Ein Auszug (`filed: false`) zieht nie eine Nummer, also wird sein `type` nie aufgelöst — er benennt die Art nur für den Fall, dass jemand ihn später zum Akteneintrag macht. (`letterhead` verschwindet erst in Plan 2, wenn das Modul den freien Brief hat — bis dahin bleibt es lauffähig.)

- [ ] **Step 5: Failing Test für das Wasserzeichen**

In `packages/documents/tests/bases-render.test.ts`:

```ts
it('zeichnet bei draft-Slots ein Wasserzeichen', async () => {
  const plain = await renderBase('a4-plain', { kind: 'plain', title: 'Test' });
  const draft = await renderBase('a4-plain', { kind: 'plain', title: 'Test', draft: true });
  expect(draft.byteLength).not.toBe(plain.byteLength);
});
```

`renderBase` ist der vorhandene Helfer der Datei; falls er anders heißt, den vorhandenen Namen verwenden.

- [ ] **Step 6: Test rot sehen**

Run: `pnpm --filter @kompass/documents test -- bases-render`
Expected: FAIL — beide PDFs sind gleich groß.

- [ ] **Step 7: Wasserzeichen in den drei Basen**

In jeder der drei `.typ`-Dateien, innerhalb der `base`-Funktion vor dem Inhalt:

```typst
#if slots.at("draft", default: false) [
  #place(
    center + horizon,
    dx: 0pt, dy: 0pt,
    rotate(-30deg, text(size: 96pt, fill: rgb(0, 0, 0, 12%), weight: "bold")[ENTWURF]),
  )
]
```

Der Text steht bewusst als Literal in der Basis und nicht in `de.json`: Basis-Vorlagen sind Typst-Dateien, die ein Verein überschreiben kann, und `no-color-literals.test.ts` scannt nur `apps/kompass/src`.

- [ ] **Step 8: Tests grün sehen**

Run: `pnpm --filter @kompass/documents test && pnpm --filter @kompass/core test -- modules`
Expected: PASS

- [ ] **Step 9: Commit**

```bash
git add packages/core/src/modules/manifest.ts packages/core/tests/modules.test.ts packages/documents
git commit -m "feat(documents): document type carries the number prefix, drafts get a watermark"
```

---

### Task 3: Modulgerüst `@kompass/module-dms` mit den Stammdatentabellen

**Files:**
- Create: `packages/modules/dms/package.json`, `tsconfig.json`, `vitest.config.ts`
- Create: `packages/modules/dms/src/schema.ts`, `src/manifest.ts`, `src/index.ts`
- Test: `packages/modules/dms/tests/manifest.test.ts`

**Interfaces:**
- Consumes: `defineModule` aus `@kompass/core`
- Produces:
  - `dmsModule: ModuleManifest` (key `dms`, `dependsOn: ['contacts']`, Rechte `dms.view`, `dms.create`, `dms.file`, `dms.void`, `dms.deleteDraft`, `dms.manage`)
  - `documentTypes` (Spalten `key`, `label`, `prefix`, `defaultDirection`, `retentionClass`, `defaultFolder`, `isActive`, `sortOrder`)
  - `documentFolders` (`path`, `createdAt`)
  - `documentRules` (`id`, `matchField`, `matchContains`, `thenTypeKey`, `thenFolder`, `isActive`, `sortOrder`)
  - Zeilentypen `DocumentTypeRow`, `DocumentFolderRow`, `DocumentRuleRow`

- [ ] **Step 1: Paket anlegen**

`packages/modules/dms/package.json` — wortgleich zu `packages/modules/contacts/package.json`, nur `"name": "@kompass/module-dms"`. `tsconfig.json` und `vitest.config.ts` ebenso aus `contacts` übernehmen.

- [ ] **Step 2: Failing Test fürs Manifest**

`packages/modules/dms/tests/manifest.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { dmsModule } from '../src/manifest';

describe('dms module', () => {
  it('hat den Schlüssel dms und hängt an den Kontakten', () => {
    expect(dmsModule.key).toBe('dms');
    expect(dmsModule.dependsOn).toContain('contacts');
  });

  it('führt die sechs Rechte der Akte', () => {
    expect(dmsModule.permissions).toEqual([
      'dms.view', 'dms.create', 'dms.file', 'dms.void', 'dms.deleteDraft', 'dms.manage',
    ]);
  });
});
```

- [ ] **Step 3: Test rot sehen**

Run: `pnpm --filter @kompass/module-dms test`
Expected: FAIL — `src/manifest.ts` existiert nicht.

- [ ] **Step 4: Stammdatentabellen schreiben**

`packages/modules/dms/src/schema.ts`:

```ts
import { index, integer, sqliteTable, text } from 'drizzle-orm/sqlite-core';

/**
 * Die Klassifikation als Stammdaten, nicht als Konstanten (Prinzip 2). Die Art
 * trägt zweierlei, das sonst geraten werden müsste: das Nummernpräfix und die
 * Aufbewahrungsklasse.
 */
export const documentTypes = sqliteTable('document_types', {
  key: text('key').primaryKey(),
  label: text('label').notNull(),
  prefix: text('prefix').notNull(), // drei Großbuchstaben
  defaultDirection: text('default_direction', { enum: ['outgoing', 'incoming'] }).notNull(),
  retentionClass: text('retention_class', { enum: ['permanent', 'statutory10Y', 'statutory6Y', 'consent'] }).notNull(),
  defaultFolder: text('default_folder'),
  isActive: integer('is_active', { mode: 'boolean' }).notNull().default(true),
  sortOrder: integer('sort_order').notNull().default(0),
});

/** Ordnungsbaum der Sachakte. Pfadtabelle wie `media_folders`. */
export const documentFolders = sqliteTable('document_folders', {
  path: text('path').primaryKey(), // 'behoerden', 'behoerden/finanzamt'
  createdAt: text('created_at').notNull(),
});

/** Einsortierhilfe: belegt das Formular vor, legt nie selbst ab (Entscheidung 19). */
export const documentRules = sqliteTable(
  'document_rules',
  {
    id: text('id').primaryKey(),
    matchField: text('match_field', { enum: ['filename', 'senderName'] }).notNull(),
    matchContains: text('match_contains').notNull(),
    thenTypeKey: text('then_type_key').references(() => documentTypes.key),
    thenFolder: text('then_folder'),
    isActive: integer('is_active', { mode: 'boolean' }).notNull().default(true),
    sortOrder: integer('sort_order').notNull().default(0),
  },
  (t) => [index('document_rules_sort_idx').on(t.sortOrder)],
);

export type DocumentTypeRow = typeof documentTypes.$inferSelect;
export type DocumentFolderRow = typeof documentFolders.$inferSelect;
export type DocumentRuleRow = typeof documentRules.$inferSelect;
```

- [ ] **Step 5: Manifest schreiben**

`packages/modules/dms/src/manifest.ts`:

```ts
import { defineModule, type ModuleManifest } from '@kompass/core';

export const dmsModule: ModuleManifest = defineModule({
  key: 'dms',
  version: '0.1.0',
  dependsOn: ['contacts'],
  permissions: ['dms.view', 'dms.create', 'dms.file', 'dms.void', 'dms.deleteDraft', 'dms.manage'],
  // `icon` muss in der Whitelist in `apps/kompass/src/components/shell/sidebar.tsx` stehen.
  navigation: [{ key: 'dms.list', href: '/dms', icon: 'file', group: 'dms', permission: 'dms.view' }],
});
```

`src/index.ts` exportiert `./manifest`, `./schema`.

- [ ] **Step 6: Symbol freischalten**

In `apps/kompass/src/components/shell/sidebar.tsx` `file` in die Icon-Whitelist aufnehmen, falls es dort fehlt.

- [ ] **Step 7: Tests grün sehen**

Run: `pnpm --filter @kompass/module-dms test`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add packages/modules/dms apps/kompass/src/components/shell/sidebar.tsx
git commit -m "feat(dms): module skeleton with types, folders and rules"
```

---

### Task 4: Die Akte zieht um — `documents` und `document_links` ins Modul

Der große Schritt. Danach kennt der Kern keine Ablage mehr.

**Files:**
- Modify: `packages/modules/dms/src/schema.ts` (`documents`, `documentLinks`)
- Modify: `packages/core/src/db/schema.ts` (`documents` entfällt)
- Create: `packages/modules/dms/src/service.ts` (Lesen, Storno, Nummernvergabe)
- Modify: `packages/core/src/documents/service.ts` (nur noch `exportDocument`, `listDocumentBases`, `prepare`, `buildContext`)
- Modify: `packages/core/src/permissions/core.ts`
- Modify: `apps/kompass/src/modules.ts`
- Test: `packages/modules/dms/tests/service.test.ts`, `packages/core/tests/documents.test.ts`

**Interfaces:**
- Consumes: `documentTypes` (Task 3), `mediaAssets.checksum` (Task 1)
- Produces:
  - `documents` mit `phase`, `direction`, `sourceKind`, `typeKey`, `number` (nullable), `subject`, `documentDate`, `folder`, `draftBody`, `templateKey` (nullable), `inputSnapshot` (nullable), `assetId` (nullable), `status`, `voidedAt`, `voidedByUserId`, `voidReason`, `createdByUserId`, `createdAt`, `updatedAt`
  - `documentLinks` (`id`, `documentId`, `entityType`, `entityId`, `role`, `createdAt`; unique über die vier fachlichen Spalten)
  - `DocumentRow`, `DocumentLinkRow`, `DocumentRecord = DocumentRow & { links: DocumentLinkRow[] }`
  - `nextDocumentNumber(db: DbOrTx, prefix: string, year: number): string`
  - `listDocuments(deps, ctx, input) → Result<{ documents: DocumentRecord[]; total: number }>`
  - `getDocument(deps, ctx, id) → Result<{ record: DocumentRecord; bytes: Uint8Array; filename: string }>`
  - `voidSchema` = `{ id: string; reason: string }`; `voidDocument(deps, ctx, input) → Result<DocumentRecord>`
  - Kernrecht `documents.export`; `documents.create` und `documents.view` entfallen

- [ ] **Step 1: Failing Test für die neue Tabellenform**

`packages/modules/dms/tests/service.test.ts`:

```ts
import { coreModule, createTestDeps } from '@kompass/core';
import { contactsModule } from '@kompass/module-contacts';
import { describe, expect, it } from 'vitest';
import { dmsModule } from '../src/manifest';
import { documents } from '../src/schema';
import { listDocuments } from '../src/service';

function setup() {
  const deps = createTestDeps({ manifests: [coreModule, contactsModule, dmsModule] });
  return { deps, ctx: deps.testing.adminContext() };
}

describe('documents', () => {
  it('führt Entwürfe ohne Nummer und festgeschriebene mit', () => {
    const { deps } = setup();
    deps.db.insert(documents).values({
      id: 'doc-draft', phase: 'draft', direction: 'outgoing', sourceKind: 'generated',
      typeKey: 'letter', number: null, subject: 'Entwurf', documentDate: '2026-09-10',
      draftBody: '# Hallo', createdByUserId: 'user-1', createdAt: '2026-09-10T00:00:00.000Z',
      updatedAt: '2026-09-10T00:00:00.000Z',
    }).run();
    const row = deps.db.select().from(documents).get();
    expect(row?.number).toBeNull();
    expect(row?.assetId).toBeNull();
  });

  it('listet nur, wer dms.view hat', async () => {
    const { deps } = setup();
    const result = await listDocuments(deps, deps.testing.contextWithout('dms.view'), {});
    expect(result.ok).toBe(false);
  });
});
```

Die Helfer `deps.testing.adminContext()` und `contextWithout(...)` heißen im Repo möglicherweise anders — die in `packages/modules/contacts/tests/service.test.ts` verwendeten Namen übernehmen.

- [ ] **Step 2: Test rot sehen**

Run: `pnpm --filter @kompass/module-dms test`
Expected: FAIL — `documents` und `listDocuments` existieren im Modul nicht.

- [ ] **Step 3: Tabellen ins Modulschema**

In `packages/modules/dms/src/schema.ts` ergänzen:

```ts
/**
 * Ein Eintrag ist eine Datei plus Metadaten — erzeugt oder eingegangen
 * (Entscheidung 5). Ein Entwurf hat weder Nummer noch Datei; er ist
 * Arbeitsmaterial und trägt seinen Text in `draftBody`.
 */
export const documents = sqliteTable(
  'documents',
  {
    id: text('id').primaryKey(),
    phase: text('phase', { enum: ['draft', 'issued'] }).notNull().default('draft'),
    direction: text('direction', { enum: ['outgoing', 'incoming'] }).notNull().default('outgoing'),
    sourceKind: text('source_kind', { enum: ['generated', 'uploaded'] }).notNull().default('generated'),
    typeKey: text('type_key').notNull().references(() => documentTypes.key),
    number: text('number'),
    subject: text('subject').notNull().default(''),
    /** Datum **auf** dem Dokument; löst die Frist aus, nicht `createdAt`. */
    documentDate: text('document_date').notNull(),
    folder: text('folder'), // null = Eingangskorb
    /** Markdown des Entwurfs; beim Festschreiben geleert (Entscheidung 4). */
    draftBody: text('draft_body'),
    templateKey: text('template_key'),
    inputSnapshot: text('input_snapshot'), // JSON, nur `generated`
    assetId: text('asset_id'),
    status: text('status', { enum: ['issued', 'voided'] }).notNull().default('issued'),
    voidedAt: text('voided_at'),
    voidedByUserId: text('voided_by_user_id'),
    voidReason: text('void_reason'),
    createdByUserId: text('created_by_user_id').notNull(),
    createdAt: text('created_at').notNull(),
    updatedAt: text('updated_at').notNull(),
  },
  (t) => [
    uniqueIndex('documents_number_idx').on(t.number),
    index('documents_folder_idx').on(t.folder),
    index('documents_phase_idx').on(t.phase),
  ],
);

/** Mehrere Bezüge je Dokument, jeder mit Rolle (Entscheidung 6). */
export const documentLinks = sqliteTable(
  'document_links',
  {
    id: text('id').primaryKey(),
    documentId: text('document_id').notNull().references(() => documents.id),
    /** Generisch wie bei `mediaReferences` — das Modul kennt keine fremden Entitäten. */
    entityType: text('entity_type').notNull(),
    entityId: text('entity_id').notNull(),
    role: text('role', { enum: ['sender', 'recipient', 'about'] }).notNull(),
    createdAt: text('created_at').notNull(),
  },
  (t) => [
    uniqueIndex('document_links_unique_idx').on(t.documentId, t.entityType, t.entityId, t.role),
    index('document_links_entity_idx').on(t.entityType, t.entityId),
  ],
);

export type DocumentRow = typeof documents.$inferSelect;
export type DocumentLinkRow = typeof documentLinks.$inferSelect;
```

`uniqueIndex` und `index` mitimportieren. In SQLite ist ein Unique-Index über eine nullable Spalte für `NULL` nicht bindend — genau richtig: beliebig viele Entwürfe ohne Nummer, aber jede Nummer nur einmal.

- [ ] **Step 4: Aus dem Kernschema entfernen**

`documents` in `packages/core/src/db/schema.ts` löschen. Der Kern verweist danach nicht mehr darauf; die Fremdschlüssel auf `users` und `media_assets` entfallen, weil ein Modul nicht in Kerntabellen hineinreferenziert (`createdByUserId` und `assetId` bleiben Textspalten ohne FK — dasselbe Muster wie `contact_roles.role`).

- [ ] **Step 5: Services ins Modul kopieren**

`packages/modules/dms/src/service.ts` bekommt aus `packages/core/src/documents/service.ts`: `nextDocumentNumber`, `toRecord`, `listDocuments`, `getDocument`, `voidDocument`. Dabei:

- Rechte umstellen: `documents.view` → `dms.view`, `documents.create` → `dms.void` bei `voidDocument`.
- `DocumentRecord` um `links: DocumentLinkRow[]` erweitern; `toRecord` lädt sie mit.
- `listDocuments` bekommt die Filter aus § 6.3 als Zod-Schema:

```ts
export const documentListSchema = z.object({
  direction: z.enum(['outgoing', 'incoming']).optional(),
  phase: z.enum(['draft', 'issued']).optional(),
  typeKey: z.string().min(1).optional(),
  folder: z.string().nullable().optional(), // null = Eingangskorb
  inbox: z.boolean().optional(),
  linkedTo: z.object({ entityType: z.string().min(1), entityId: z.string().min(1) }).optional(),
  text: z.string().trim().min(1).optional(), // Betreff oder Nummer
  limit: z.number().int().min(1).max(200).default(50),
  offset: z.number().int().min(0).default(0),
});
```

- `nextDocumentNumber` bleibt in Signatur und Format unverändert, liest aber `documents` aus dem Modulschema.

- [ ] **Step 6: Kern aufräumen**

In `packages/core/src/documents/service.ts` `renderDocument`, `listDocuments`, `getDocument`, `voidDocument`, `nextDocumentNumber` und `toRecord` löschen. `exportDocument` und `listDocumentBases` bleiben, beide prüfen künftig `documents.export`. In `packages/core/src/permissions/core.ts` `documents.create` und `documents.view` durch `documents.export` ersetzen. Alle Exporte in `packages/core/src/index.ts` entsprechend anpassen; `prepare` und `buildContext` exportieren, weil das Modul sie braucht.

- [ ] **Step 7: Modul installieren**

In `apps/kompass/src/modules.ts` `dmsModule` importieren und an `installedModules` anhängen. In `apps/kompass/tests/` die E2E-Erwartung der aktiven Modulanzahl anpassen (dieselbe Stelle wie in Commit `133e371`).

- [ ] **Step 8: Tests grün sehen**

Run: `pnpm --filter @kompass/module-dms test && pnpm --filter @kompass/core test`
Expected: PASS

- [ ] **Step 9: Commit**

```bash
git add packages/modules/dms packages/core/src apps/kompass/src/modules.ts apps/kompass/tests
git commit -m "feat(dms): the file moves out of the core, which keeps only the pipeline"
```

---

### Task 5: Migration mit Datenumzug

**Files:**
- Create: `packages/core/src/db/migrations/0014_dms.sql` (erzeugt)
- Create: `packages/core/src/db/migrations/0015_dms_links.sql` (handgeschrieben)
- Modify: `packages/core/src/db/migrations/meta/_journal.json`
- Test: `packages/modules/dms/tests/migration.test.ts`

**Interfaces:**
- Consumes: Schema aus Task 3 und 4
- Produces: eine Datenbank, in der vorhandene Dokumentzeilen Nummer und Datei behalten und ihr `entityType`/`entityId`-Paar als `about`-Link führen

- [ ] **Step 1: Failing Test schreiben**

`packages/modules/dms/tests/migration.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { readFileSync } from 'node:fs';

describe('migration 0015', () => {
  it('zieht die alten Bezüge als about-Link um', () => {
    const sql = readFileSync('../../core/src/db/migrations/0015_dms_links.sql', 'utf8');
    expect(sql).toContain('INSERT INTO `document_links`');
    expect(sql).toContain("'about'");
    expect(sql).toContain('WHERE');
  });
});
```

Der Test prüft die Datei, nicht die Ausführung: Die Ausführung deckt der Migrationslauf in `createTestDeps()` ab, der in jedem anderen Test dieses Moduls stattfindet — schlägt die Migration fehl, wird die ganze Suite rot.

- [ ] **Step 2: Test rot sehen**

Run: `pnpm --filter @kompass/module-dms test -- migration`
Expected: FAIL — Datei fehlt.

- [ ] **Step 3: Schemamigration erzeugen**

Run: `pnpm --filter @kompass/core db:generate`

Die erzeugte Datei nach `0014_dms.sql` umbenennen und den Journal-Tag setzen. Prüfen, dass sie die drei neuen Tabellen anlegt und `documents` über den SQLite-üblichen Tabellenneubau (`__new_documents`, `INSERT INTO ... SELECT`, `DROP`, `RENAME`) umbaut — **mit** `INSERT`, sonst gehen vorhandene Zeilen verloren. Fehlt der `INSERT`, die Datei nicht editieren, sondern das Schema so lange anpassen, bis `db:generate` es richtig erzeugt.

- [ ] **Step 4: Datenmigration schreiben**

`packages/core/src/db/migrations/0015_dms_links.sql`, nach dem Muster von `0006_i18n_locales.sql`:

```sql
INSERT INTO `document_links` (`id`, `document_id`, `entity_type`, `entity_id`, `role`, `created_at`)
SELECT lower(hex(randomblob(16))), `id`, `entity_type`, `entity_id`, 'about', `created_at`
FROM `documents`
WHERE `entity_type` IS NOT NULL AND `entity_id` IS NOT NULL;
```

Die IDs sind hier keine ULIDs, weil SQLite keine erzeugen kann; das ist der einzige Ort, an dem das vorkommt, und die Zeilen stammen aus einer Installation mit höchstens einer Handvoll Dokumenten. Journal-Eintrag mit `"tag": "0015_dms_links"` ergänzen.

- [ ] **Step 5: Alte Spalten entfernen**

`entity_type` und `entity_id` in `0016_dms_drop_entity.sql` (erzeugt, nachdem die Spalten aus dem Schema entfernt wurden) fallen lassen. Reihenfolge zwingend: erst umziehen, dann löschen.

- [ ] **Step 6: Tests grün sehen**

Run: `pnpm --filter @kompass/module-dms test && pnpm --filter @kompass/core test`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add packages/core/src/db/migrations packages/modules/dms
git commit -m "feat(dms): migration that keeps every filed document and its reference"
```

---

## Abschluss

- [ ] `pnpm typecheck && pnpm test`
- [ ] `pnpm verify` (braucht Docker)
- [ ] Plan 2 (`2026-09-10-dms-2-entwurf.md`) beginnen
