/**
 * Die Regelprüfung der Vorschläge (F5) — seit F8a auf neutralem Boden
 * (`src/rules-pure.ts`), weil auch `allocation/` sie braucht. Hier nur
 * weitergereicht, damit `suggest/` seine fünf Bausteine an einer Stelle zeigt.
 */
export { normalizeText, ruleMatches, type RuleConditions, type RuleTarget } from '../../rules-pure';
