# CLAUDE.md

Regeln, Ziel und Befehle stehen in `AGENTS.md` — bitte zuerst lesen. Specs, Pläne und Design-Referenz liegen in `docs/intern/` (`specs/`, `plans/`, `design/`) — ein eigenes privates Git, im öffentlichen Repo ignoriert.

Schnellbefehle: `pnpm install` · `pnpm test` · `pnpm typecheck` · `pnpm --filter @kompass/core db:generate` · `pnpm --filter @kompass/app e2e` · `docker build -t kompass-local .` · Betrieb: `docs/handbuch/betrieb.md`.
